import React from 'react';
import './Form.css';

const Form: React.FC = () => {
    return (
        <form>
            <input type="text" />
            <input type="submit" value="확인" />
        </form>
    );
}

export default Form;
